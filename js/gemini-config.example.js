/**
 * Copia este archivo a `gemini-config.local.js` y pega tu API key.
 *
 * 1. Abre https://aistudio.google.com/apikey
 * 2. Inicia sesión con tu cuenta Google
 * 3. Clic en «Create API key»
 * 4. Pega la key abajo
 *
 * En Calma se usa solo para MEJORAR GUIONES de programas (texto).
 * No es obligatorio para la voz — la voz usa Azure o el navegador.
 */
export const GEMINI_API_KEY = ''
